import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Ensure the ID matches the one in your `public/index.html` file
ReactDOM.render(<App />, document.getElementById('root'));
